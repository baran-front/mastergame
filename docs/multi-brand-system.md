# سیستم چند برندی (Multi-Brand System)

این مستند نحوه کارکرد سیستم چند برندی پروژه را توضیح می‌دهد.

---

## فهرست مطالب

1. [مقدمه](#مقدمه)
2. [ساختار فایل‌ها](#ساختار-فایلها)
3. [ساختار فایل تنظیمات برند](#ساختار-فایل-تنظیمات-برند)
4. [نحوه تولید فایل برند](#نحوه-تولید-فایل-برند)
5. [تغییر برند فعال](#تغییر-برند-فعال)
6. [افزودن برند جدید](#افزودن-برند-جدید)
7. [استفاده از متغیرهای برند در کد](#استفاده-از-متغیرهای-برند-در-کد)
8. [نکات مهم](#نکات-مهم)

---

## مقدمه

سیستم چند برندی به شما امکان می‌دهد از یک کدبیس واحد برای چندین برند مختلف استفاده کنید. هر برند می‌تواند تنظیمات اختصاصی خود را شامل:

- نام برند
- آدرس API
- لوگو
- تم رنگی (روشن و تیره)
- سایر تنظیمات سفارشی

داشته باشد.

---

## ساختار فایل‌ها

```
mastergame-website/
├── brands/                      # 📁 پوشه فایل‌های تنظیمات برندها
│   ├── master-game.json         # تنظیمات برند مستر گیم
│   └── bakhshi-remote.json      # تنظیمات برند دیگر
│
├── public/
│   └── brands/                  # 📁 پوشه فایل‌های استاتیک برندها
│       ├── master-game/
│       │   └── logo.png         # لوگوی مستر گیم
│       └── bakhshi-remote/
│           └── logo.png         # لوگوی برند دیگر
│
├── scripts/
│   └── generate-brand.ts        # 🔧 اسکریپت تولید فایل برند
│
└── brand.ts                     # ⚠️ فایل خودکار تولید شده (ویرایش نکنید!)
```

---

## ساختار فایل تنظیمات برند

هر برند یک فایل JSON در پوشه `/brands/` دارد:

```json
{
  "name": "نام برند",
  "apiUrl": "https://api.example.com/api",
  "apiHostname": "api.example.com",
  "apiBaseUrl": "https://api.example.com",
  "apiTenant": "tenant-name",
  "apiToken": "your-jwt-token",
  "logoImg": "/brands/brand-name/logo.png",
  "theme": {
    "light": {
      "background": "#f2f2f3",
      "foreground": "#393c3e",
      "card": "#e2e5e7",
      "primary": "#a030bf",
      "secondary": "#00b4f2"
    },
    "dark": {
      "background": "#1e1e1e",
      "foreground": "#ffffff",
      "card": "#2b2b2b",
      "primary": "#a030bf",
      "secondary": "#00b4f2"
    }
  }
}
```

### توضیح فیلدها

| فیلد          | نوع    | توضیحات                        |
| ------------- | ------ | ------------------------------ |
| `name`        | string | نام نمایشی برند                |
| `apiUrl`      | string | آدرس کامل API شامل `/api`      |
| `apiHostname` | string | هاست API (بدون پروتکل)         |
| `apiBaseUrl`  | string | آدرس پایه API (بدون `/api`)    |
| `apiTenant`   | string | شناسه تنانت برای API           |
| `apiToken`    | string | توکن JWT برای احراز هویت       |
| `logoImg`     | string | مسیر لوگوی برند در پوشه public |
| `theme`       | object | تنظیمات تم رنگی                |

### ساختار تم رنگی

```json
{
  "theme": {
    "light": {
      "background": "#ffffff", // رنگ پس‌زمینه
      "foreground": "#000000", // رنگ متن
      "card": "#f5f5f5", // رنگ کارت‌ها
      "primary": "#3b82f6", // رنگ اصلی
      "secondary": "#10b981" // رنگ ثانویه
    },
    "dark": {
      "background": "#1e1e1e",
      "foreground": "#ffffff",
      "card": "#2b2b2b",
      "primary": "#3b82f6",
      "secondary": "#10b981"
    }
  }
}
```

---

## نحوه تولید فایل برند

### اسکریپت تولید

فایل `scripts/generate-brand.ts` وظیفه تولید فایل `brand.ts` را بر عهده دارد:

```typescript
// خلاصه عملکرد اسکریپت:
// 1. دریافت نام برند از آرگومان‌های خط فرمان
// 2. خواندن فایل JSON تنظیمات برند
// 3. تولید فایل brand.ts با مقادیر برند
```

### دستور اجرا

```bash
npm run generate-brand <brand-name>
```

### مثال

```bash
# تولید تنظیمات برند مستر گیم
npm run generate-brand master-game

# تولید تنظیمات برند دیگر
npm run generate-brand bakhshi-remote
```

### خروجی

پس از اجرای دستور، فایل `brand.ts` در ریشه پروژه تولید می‌شود:

```typescript
// This file is auto-generated by generate-brand.ts
// Do not edit this file manually

export const brand = {
  name: "مستر گیم",
  apiUrl: "https://api.arvinvira.com/api",
  apiHostname: "api.arvinvira.com",
  // ... سایر تنظیمات
} as const;

export type BrandT = typeof brand;
```

---

## تغییر برند فعال

برای تغییر برند فعال پروژه:

### مرحله ۱: اجرای دستور تولید

```bash
npm run generate-brand <new-brand-name>
```

### مرحله ۲: ری‌استارت سرور (در صورت نیاز)

اگر سرور در حال اجراست، آن را متوقف و مجدداً اجرا کنید

```bash
ctrl+c

npm run dev
```

### نمونه کامل

فرض کنید برند فعلی master-game است

برای تغییر به برند bakhshi-remote:

```bash

npm run generate-brand bakhshi-remote

ctrl+c

npm run dev
```

---

## افزودن برند جدید

### مرحله ۱: ایجاد فایل تنظیمات

یک فایل JSON جدید در پوشه `/brands/` ایجاد کنید:

```
brands/new-brand.json
```

محتوای فایل:

```json
{
  "name": "نام برند جدید",
  "apiUrl": "https://api.newbrand.com/api",
  "apiHostname": "api.newbrand.com",
  "apiBaseUrl": "https://api.newbrand.com",
  "apiTenant": "new-tenant",
  "apiToken": "your-jwt-token-here",
  "logoImg": "/brands/new-brand/logo.png",
  "theme": {
    "light": {
      "background": "#ffffff",
      "foreground": "#333333",
      "card": "#f0f0f0",
      "primary": "#0066cc",
      "secondary": "#00cc66"
    },
    "dark": {
      "background": "#121212",
      "foreground": "#ffffff",
      "card": "#1e1e1e",
      "primary": "#3399ff",
      "secondary": "#33ff99"
    }
  }
}
```

### مرحله ۲: افزودن فایل‌های استاتیک

پوشه و فایل‌های مورد نیاز را در `/public/brands/` ایجاد کنید:

```
public/
└── brands/
    └── new-brand/
        └── logo.png
```

### مرحله ۳: تولید فایل برند

```bash
npm run generate-brand new-brand
```

### مرحله ۴: بررسی و تست

```bash
npm run dev
```

---

## استفاده از متغیرهای برند در کد

### ایمپورت

```typescript
import { brand } from "@/brand";
```

### دسترسی به مقادیر

```typescript
console.log(brand.name); // "مستر گیم"

// آدرس API
console.log(brand.apiUrl); // "https://api.arvinvira.com/api"

// لوگو
console.log(brand.logoImg); // "/brands/master-game/logo.png"

// تنظیمات تم
console.log(brand.theme.light); // { background: "#f2f2f3", ... }
console.log(brand.theme.dark); // { background: "#1e1e1e", ... }
```

### مثال استفاده در کامپوننت

```tsx
import { brand } from "@/brand";
import Image from "next/image";

function Header() {
  return (
    <header>
      <Image src={brand.logoImg} alt={brand.name} width={120} height={40} />
      <h1>{brand.name}</h1>
    </header>
  );
}
```

### مثال استفاده در API

```typescript
import { brand } from "@/brand";

async function fetchData() {
  const response = await fetch(brand.apiUrl + "/v1/products", {
    headers: {
      Tenant: brand.apiTenant,
      Authorization: `Bearer ${brand.apiToken}`,
    },
  });
  return response.json();
}
```

### استفاده در تم

```typescript
import { brand } from "@/brand";

// تنظیم CSS Variables
document.documentElement.style.setProperty(
  "--primary-color",
  brand.theme.light.primary
);
```

---

## نکات مهم

### ⚠️ هشدارها

1. **فایل `brand.ts` را ویرایش نکنید!**

   - این فایل به صورت خودکار تولید می‌شود
   - هرگونه تغییر دستی با اجرای مجدد دستور generate-brand پاک خواهد شد

2. **فایل `brand.ts` را در `.gitignore` قرار ندهید**

   - این فایل باید در ریپازیتوری باشد تا بیلد production کار کند

3. **قبل از بیلد، برند صحیح را تولید کنید**
   ```bash
   npm run generate-brand master-game
   npm run build
   ```

### ✅ بهترین روش‌ها

1. **نام‌گذاری فایل‌ها با kebab-case**

   ```
   ✅ brands/my-brand.json
   ❌ brands/myBrand.json
   ❌ brands/my_brand.json
   ```

2. **مطابقت نام پوشه‌ها**

   - نام فایل JSON و نام پوشه در public باید یکسان باشند:

   ```
   brands/my-brand.json
   public/brands/my-brand/logo.png
   ```

3. **استفاده از رنگ‌های هگز ۶ رقمی**

   ```json
   ✅ "#a030bf"
   ❌ "#a03"
   ❌ "rgb(160, 48, 191)"
   ```

4. **تست هر دو تم**
   - پس از تغییر تنظیمات تم، هم تم روشن و هم تم تیره را تست کنید

---

## عیب‌یابی

### خطای "Config file not found"

```
Error: Config file not found: /path/to/brands/xyz.json
```

**راه‌حل:** مطمئن شوید نام برند صحیح است و فایل JSON در پوشه `/brands/` وجود دارد.

### خطای "Invalid JSON"

```
Error processing config: SyntaxError: Unexpected token
```

**راه‌حل:** ساختار JSON فایل تنظیمات را بررسی کنید. می‌توانید از ابزارهای آنلاین JSON Validator استفاده کنید.

### لوگو نمایش داده نمی‌شود

**راه‌حل:**

1. مطمئن شوید فایل لوگو در مسیر صحیح قرار دارد
2. مسیر `logoImg` در فایل JSON را بررسی کنید
3. فرمت تصویر (png, jpg, svg) پشتیبانی شود

---

## خلاصه دستورات

| دستور                                 | توضیحات                     |
| ------------------------------------- | --------------------------- |
| `npm run generate-brand master-game`  | تولید تنظیمات برند مستر گیم |
| `npm run generate-brand <brand-name>` | تولید تنظیمات برند دلخواه   |
| `npm run dev`                         | اجرای سرور توسعه            |
| `npm run build`                       | بیلد پروژه برای production  |

---

## نمونه کامل: افزودن برند جدید

```bash
# Start

# ۱. ایجاد فایل تنظیمات
# فایل brands/my-store.json را با محتوای مناسب ایجاد کنید

# ۲. ایجاد پوشه فایل‌های استاتیک
mkdir -p public/brands/my-store

# ۳. کپی لوگو
cp path/to/logo.png public/brands/my-store/logo.png

# ۴. تولید فایل برند
npm run generate-brand my-store

# ۵. اجرای سرور توسعه
npm run dev

# ۶. بررسی در مرورگر
# http://localhost:3000
```
